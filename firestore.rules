rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /Jobs/{docId} {
      allow read, create: if request.auth != null;
      allow write, update: if request.auth.uid == resource.data.userID;
    }
  }
}